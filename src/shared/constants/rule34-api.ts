/** Substring present in Rule34 DAPI bodies when api_key/user_id are missing or invalid. */
export const RULE34_MISSING_AUTHENTICATION_MARKER = "Missing authentication";

/** Max raw response body chars logged in main on parse failures (not sent to renderer). */
export const RULE34_RESPONSE_BODY_LOG_SNIPPET_CHARS = 500;
