# 🚀 Дорожная карта

## 📑 Оглавление

- [✅ Недавние исправления и текущий статус (ЗАВЕРШЕНО)](#-recent-fixes--current-status-completed)
- [🚀 Активная дорожная карта (Приоритетные задачи)](#-active-roadmap-priority-tasks)
- [🧭 Обновление навигации и UX](#-navigation--ux-revamp)
- [📰 Подписки / Обновления](#-subscriptions--updates)
- [🛡️ Безопасность и надежность (Укрепление)](#-security--reliability-hardening)
- [📋 Вехи](#-milestones)
- [🔧 Технические улучшения (По результатам аудита)](#-technical-improvements-from-audit)
- [🏗️ Архитектурные соображения](#-architecture-considerations)
- [🔮 Долгосрочные цели (Будущие соображения)](#-long-term-goals-future-considerations)

---

## ✅ Недавние исправления и текущий статус (ЗАВЕРШЕНО)

Мы успешно стабилизировали ядро приложения. Следующие проблемы РЕШЕНЫ:

### Инфраструктура и сборка

- ✅ Исправлена нативная сборка `better-sqlite3` в Windows (устранены несоответствия `node-gyp`, Python и версий ABI)
- ✅ Приложение успешно запускается через `npm run dev` и взаимодействует с базой данных SQLite

### База данных и схема

- ✅ Заменены несовместимые `unixepoch` и JS-даты на необработанные SQL-метки времени (мс)
- ✅ Добавлены корректные ограничения `UNIQUE` к таблице `posts` (`artistId` + `postId`) для обеспечения правильных операций UPSERT
- ✅ Добавлен столбец `sampleUrl` для прогрессивной загрузки изображений
- ✅ Система миграций (`drizzle-kit`) полностью функциональна

### Целостность данных и синхронизация

- ✅ Реализована нормализация Tag в `AddArtistModal`: Вводы типа "tag (123)" теперь обрезаются до "tag" перед сохранением/синхронизацией
- ✅ `SyncService` корректно обрабатывает `ON CONFLICT` и заполняет галерею
- ✅ Исправлена обработка меток времени: `lastChecked` теперь использует `new Date()` с правильным режимом меток времени Drizzle

### UI/UX

- ✅ Исправлены "мыльные/размытые" превью: Качество рендеринга изображений для превью скорректировано
- ✅ Реализована прогрессивная загрузка изображений: 3-уровневая система (Preview → Sample → Original) для мгновенного просмотра
- ✅ Базовая сетка галереи функциональна
- ✅ Компонент `AsyncAutocomplete` для поиска по artist/tag с поддержкой свободного ввода текста

---

## 🚀 Активная дорожная карта (Приоритетные задачи)

Мы переходим к разработке функционала. Реализуйте следующие модули:

## 🧭 Обновление навигации и UX

### Боковая панель навигации

Реализовать постоянную боковую панель с основными разделами навигации:

- **Updates** - Лента подписок (новые посты из отслеживаемых источников)
- **Browse** - Просмотр всех постов с расширенной фильтрацией
- **Favorites** - Избранное учетной записи, синхронизированное с booru
- **Tracked** - Управление artists и tags
- **Settings** - Конфигурация приложения

### Глобальная верхняя панель

Единая верхняя панель на всех страницах контента:

- **Search Bar** - Быстрый поиск по постам, tags, artists
- **Filters Panel** - Панель фильтров: рейтинг, тип медиа, tags, диапазон дат
- **Sort Controls** - Элементы управления сортировкой: по дате добавления, дате публикации, рейтингу
- **View Toggle** - Переключение вида: сетка, список, масонский макет
- **Sync Status** - Индикатор синхронизации в реальном времени с меткой времени последней синхронизации

### Доработка просмотрщика

Улучшенный полноэкранный просмотр:

- **Auto-hide Bars** - Верхняя и нижняя панели скрываются после периода бездействия
- **Tags Sheet** - Выдвижная панель справа с кликабельными tags
  - Нажмите tag, чтобы добавить фильтр (`+tag`)
  - Правый клик или клавиша-модификатор для исключения (`-tag`)
  - Визуальные индикаторы активных фильтров
- **Tooltips** - Всплывающие подсказки: сочетания клавиш и подсказки по действиям
- **Сочетания клавиш:**
  - `Esc` - Закрыть просмотрщик
  - `←/→` - Перемещение между постами
  - `F` - Переключить избранное
  - `V` - Отметить как просмотренное
  - `T` - Переключить выдвижную панель tags

### Наложения на карточки галереи

Карточки постов с информативными наложениями:

- **Viewed Badge** - Индикатор для просмотренных постов
- **Favorite Badge** - Иконка звезды для избранных постов
- **Rating Badge** - Визуальный индикатор (Safe/Questionable/Explicit)
- **Media Type Badge** - Иконка для изображений/видео контента

### Прогрессивная загрузка изображений

Оптимизированная стратегия загрузки:

- **Preview URL** - Размытое превью низкого разрешения (мгновенное отображение в галерее)
- **Sample URL** - Образец среднего разрешения (загружается в галерее)
- **File URL** - Оригинал полного разрешения (загружается только в просмотрщике)

## 📰 Подписки / Обновления

### Вкладка "Лента"

Единая лента, отображающая все новые посты:

- **All New Posts** - Объединенная лента из всех отслеживаемых источников
- **Filters** - Применить фильтры по tags, рейтингу, типу медиа
- **Infinite Scroll** - Прогрессивная загрузка по мере прокрутки пользователем
- **Mark as Read** - Групповая отметка постов как просмотренных

### Вкладка "Создатели"

Просмотр списка/плиток создателей с количеством новых постов:

- **Creator List** - Сетка или список отслеживаемых artists
- **New Count Badge** - Отображение количества непросмотренных постов для каждого создателя
- **Quick Actions** - Быстрые действия: синхронизация, восстановление, просмотр галереи для каждого создателя
- **Filters** - Фильтр создателей по типу (tag/uploader)

### Фильтры

Расширенная фильтрация в разделе Updates:

- **Tag Filters** - Включение/исключение определенных tags
- **Rating Filter** - Фильтр рейтинга: Safe, Questionable, Explicit
- **Media Type Filter** - Фильтр по типу медиа: Изображения, Видео или оба
- **Date Range** - Диапазон дат: фильтр по дате публикации

## 🛡️ Безопасность и надежность (Укрепление)

### Безопасность API Key

Повышенная безопасность для учетных данных API:

- **Renderer Isolation** - Renderer Process никогда не получает необработанный API key
- **Safe Storage** - Использование API `safeStorage` Electron (Windows Credential Manager, macOS Keychain, Linux libsecret)
- **AES-GCM Policy** - Шифрование API keys в покое с использованием AES-GCM
- **Threat Model** - Украденный файл базы данных не раскрывает API key в открытом виде

**Статус:** ✅ **ЗАВЕРШЕНО:** API keys зашифрованы в покое, дешифровка только в Main Process.

### Резервное копирование и целостность базы данных

Комплексная защита базы данных:

- **Backup System** - Система резервного копирования: ручное и автоматическое резервное копирование перед обслуживанием
- **Restore Flow** - Процесс восстановления: восстановление из резервной копии с автоматическим перезапуском
- **Integrity Check** - Проверка целостности: выполнение `PRAGMA integrity_check` и отображение результатов
- **Retention Policy** - Политика хранения: сохранять N последних резервных копий, автоматическая очистка старых резервных копий

**Статус:** ✅ **ФАЗА 1 ЗАВЕРШЕНА:** Реализовано ручное резервное копирование/восстановление. В будущем: автоматическое резервное копирование, UI для проверки целостности, политика хранения.

### Автоматическое обслуживание

Неблокирующее обслуживание базы данных:

- **Worker Thread** - Все ресурсоемкие операции выполняются в рабочем потоке базы данных
- **Non-blocking** - Неблокирующие: операции обслуживания не замораживают UI
- **Progress Events** - События прогресса: обновления прогресса в реальном времени для длительных операций
- **Scheduled Runs** - Запланированные запуски: автоматическое обслуживание при запуске или через периодические интервалы

**Статус:** ✅ **ЗАВЕРШЕНО:** Операции с базой данных выполняются в рабочем потоке. В будущем: запланированные запуски обслуживания.

## 📋 Вехи

### MVP (Минимально жизнеспособный продукт)

Основные функции для первоначального выпуска:

- ✅ **Navigation & Sidebar** - Постоянная боковая панель с основными разделами, кнопкой синхронизации, выходом из системы (реализовано)
- ✅ **Global Top Bar** - Строка поиска, выпадающий список сортировки, кнопка фильтров, UI переключения вида (полностью реализовано в `GlobalTopBar.tsx`, фильтрация на бэкенде ожидает реализации)
- ✅ **Viewer Polish** - Полноэкранный просмотрщик с сочетаниями клавиш, загрузкой, избранным (реализовано)
- ✅ **Progressive Loading** - Прогрессивная загрузка: Preview → Sample → Original (реализовано)
- ⏳ **Автосинхронизация при запуске** - Переключатель для автоматической синхронизации при запуске приложения (запланировано)
- ✅ **Архитектура базы данных** - Прямой синхронный доступ через `better-sqlite3` с режимом WAL (завершено)
- ✅ **Download Manager** - Загрузчик: индивидуальная загрузка файлов с отслеживанием прогресса (реализовано)
- ✅ **Favorites System** - Система избранного: отметка и управление избранными постами (реализовано)

### Следующая фаза

Расширенные функции после MVP:

- ✅ **Favorites System** - Система избранного: отметка и управление избранными постами (реализовано)
- ✅ **Tag Autocomplete** - Автозаполнение tags: локальный и удаленный поиск tags с автозаполнением (реализовано)
- ⏳ **Favorites Sync** - Синхронизация избранного учетной записи с booru (запланировано)
- ⏳ **Основа для Playlists** - Базовые таблицы и структура UI для Playlists (запланировано)
- ⏳ **Периодическая синхронизация** - Настраиваемая интервальная синхронизация во время работы приложения (запланировано)
- ⏳ **Наложения на карточки** - Значки просмотренных, избранных, рейтинга, типа медиа (частично реализовано - значки просмотренных и избранных существуют)

### Более поздняя фаза

Расширенные функции для будущих выпусков:

- ⏳ **Smart Playlists** - Умные Playlists: автоматическое заполнение Playlists на основе правил tags
- ⏳ **Нормализованный индекс Tag** - Полнотекстовый поиск по tags (виртуальные таблицы FTS5)
- ⏳ **Расширенное кеширование** - Интеллектуальное управление кешем с ограничениями размера
- ⏳ **Поддержка прокси** - Опциональная конфигурация прокси для запросов API
- ⏳ **Multi-Booru** - Шаблон провайдера для нескольких источников booru

## 🔧 Технические улучшения (По результатам аудита)

### Опыт разработчика

- ⏳ **HMR для Main Process:** Реализовать автоперезапуск для изменений Main Process (nodemon/watch mode)
  - **Текущее состояние:** Требуется ручной перезапуск
  - **Цель:** Автоматический перезапуск при изменениях файлов Main Process

### Безопасность и конфиденциальность

- ⏳ **Безопасный режим / NSFW Filter:** Реализовать размытие/фильтрацию контента на основе настроек
  - **Текущее состояние:** Схема базы данных включает поле `isSafeMode` в таблице настроек, но логика размытия еще не реализована в UI Components
  - **Цель:** Реализовать логику размытия в `PostCard.tsx` и Components просмотрщика на основе настройки `isSafeMode`
- ⏳ **Возрастное ограничение:** Реализовать оверлей подтверждения 18+
  - **Текущее состояние:** Схема базы данных включает поле `isAdultConfirmed`, но оверлей подтверждения еще не реализован
  - **Цель:** Одноразовый диалог подтверждения, который устанавливает флаг `isAdultConfirmed` в базе данных

### Портативность

- ⏳ **Портативный режим:** Поддержка относительных путей к базе данных и активам
  - **Текущее состояние:** Использует абсолютные пути через `app.getPath("userData")`
  - **Цель:** Проверка флага/переменной окружения `portable`, использование относительных путей

### Качество кода

- ⏳ **Централизованная очистка ввода:** Создать общую утилиту валидации
  - **Текущее состояние:** Валидация Zod для каждого обработчика (децентрализованная)
  - **Цель:** `src/main/lib/validation.ts` с общими схемами

### Меры против ботов

- ⏳ **Рандомизация запросов:** Реализовать ротацию User-Agent и джиттер запросов
  - **Текущее состояние:** Статический User-Agent, фиксированные задержки
  - **Цель:** Рандомизированные User-Agents, переменные задержки с джиттером

### Оптимизация базы данных

- ⏳ **Виртуальные таблицы FTS5:** Полнотекстовый поиск для поиска по tags
  - **Текущее состояние:** Только стандартные индексы (`artistIdIdx`, `isViewedIdx`, etc.)
  - **Цель:** Виртуальная таблица FTS5 для эффективного поиска по tags

### Обработка ошибок

- ⏳ **Стандартизированные ответы об ошибках IPC:** Удобные для пользователя сообщения об ошибках
  - **Текущее состояние:** Некоторые обработчики возвращают необработанные ошибки, inconsistent formatting
  - **Цель:** Единообразные, удобные для пользователя сообщения об ошибках во всех обработчиках IPC

### Производительность видео

- ⏳ **Явное аппаратное ускорение:** Настроить `webPreferences` для оптимального воспроизведения видео
  - **Текущее состояние:** Обработка видео существует, полагается на настройки Electron по умолчанию
  - **Цель:** Явные флаги `enableWebGL` и аппаратного ускорения

### A. Фильтры (Расширенный поиск) [Приоритет: Высокий] 🚧 UI готов, бэкенд ожидает

**Цель:** Позволить пользователям уточнять вид галереи.

**UI:**

- ✅ **Global Top Bar:** Полностью реализовано в `GlobalTopBar.tsx` и используется в `AppLayout.tsx`
- ✅ Строка поиска в Global Top Bar (UI готов, функциональность поиска ожидает)
- ✅ Выпадающий список сортировки в Global Top Bar (UI готов, логика сортировки ожидает)
- ✅ Переключатель вида (Сетка/Список) в Global Top Bar (UI готов, переключение вида ожидает)
- ✅ Кнопка фильтров в Global Top Bar (UI готов, панель фильтров ожидает)

**Функциональность:**

- ⏳ Фильтр по **Рейтингу** (Safe, Questionable, Explicit) - UI готов, фильтрация на бэкенде ожидает
- ⏳ Фильтр по **Типу медиа** (Изображение против Видео) - UI готов, фильтрация на бэкенде ожидает
- ⏳ Фильтр по **Tags** (Локальный поиск по загруженным постам) - UI готов, фильтрация на бэкенде ожидает
- ⏳ Сортировка по: Дате добавления (Новые/Старые), Дате публикации - UI готов, сортировка на бэкенде ожидает
- ⏳ Функциональность поиска - UI готов, логика поиска ожидает

**Примечания по реализации:**

- Использовать запросы Drizzle ORM с корректной фильтрацией
- Поддерживать типобезопасность с помощью Zod/TypeScript
- Обновлять UI State через инвалидацию React Query
- Подключить элементы управления `GlobalTopBar` к логике фильтрации `ArtistGallery`

**Статус:** UI Global Top Bar полностью реализован и виден в приложении. Логика фильтрации, сортировки и поиска на бэкенде должна быть подключена к элементам управления UI.

---

### B. Download Manager [Приоритет: Высокий] ✅ Реализовано (Частично)

**Цель:** Позволить сохранять файлы полного разрешения в локальную файловую систему (вне внутреннего кеша базы данных приложения).

**Возможности:**

- ✅ Кнопка "Download Original" в окне просмотра поста (реализовано в `ViewerDialog`)
- ✅ **Queue System:** Система очереди: обработка загрузок в фоновом/Main Process с отслеживанием прогресса
- ✅ **Progress Events:** События прогресса: обновления прогресса загрузки в реальном времени через IPC события (`onDownloadProgress`)
- ✅ **File Management:** Управление файлами: открыть загруженный файл в папке (`openFileInFolder`)
- ⏳ "Загрузить все" для текущего фильтра/artist (запланировано)
- ⏳ **Settings:** Позволить выбирать директорию загрузки по умолчанию (запланировано)

**Примечания по реализации:**

- Загрузки выполняются в Main Process (файловый ввод/вывод) через `registerFileHandlers`
- IPC события предоставляют обновления прогресса загрузки
- Управление очередью предотвращает перегрузку системы
- Настройки загрузки могут храниться в таблице settings (будущее улучшение)

**Статус:** ✅ Базовая функциональность загрузки реализована. Индивидуальные загрузки файлов работают с отслеживанием прогресса. Пакетная загрузка и настройки директории по умолчанию запланированы для будущих выпусков.

---

### C. Playlists / Коллекции [Приоритет: Средний] ⏳ Не начато

**Цель:** Создавать курируемые коллекции постов независимо от Artists/Trackers.

#### Фаза 1: MVP

1.  **База данных:**

    -   [ ] Новая таблица `playlists` (`id`, `name`, `created_at`)
    -   [ ] Новая таблица `playlist_posts` (`playlist_id`, `post_id`, `added_at`)

2.  **Взаимодействие с UI:**

    -   [ ] Кнопка "⭐ Add to playlist" на карточке поста (открывает Popover: Список playlists + "Создать новый")
    -   [ ] Новая страница/вкладка: "Playlists" (в Sidebar)
    -   [ ] Просмотр Playlist: Просмотр сетки постов внутри Playlist

3.  **Логика:**
    -   [ ] Фильтрация внутри Playlist (Поиск tags по `LIKE`)
    -   [ ] Сортировка по `addedAt`
    -   [ ] Удаление поста из Playlist
    -   [ ] Удаление/переименование Playlist

**Примечания по реализации:**

- Следовать существующим паттернам базы данных (Drizzle ORM, типобезопасность)
- Использовать IPC для всех операций с базой данных
- Поддерживать разделение ответственности (Renderer ↔ Main)

**Статус:** В схеме нет таблиц playlists, код, связанный с playlists, не реализован.

#### Фаза 2: Будущие улучшения (Не сейчас)

- Перетаскивание для сортировки
- Умные/Динамические Playlists (автоматическое заполнение на основе tags)
- Экспорт/импорт JSON

---

## 🏗️ Архитектурные соображения

### Принципы проектирования

- **KISS & YAGNI:** Keep It Simple, Stupid. You Aren't Gonna Need It.
- **SOLID:** Единая ответственность, Открытость/Закрытость, Инверсия зависимостей
- **DRY:** Don't Repeat Yourself
- **Типобезопасность:** Строгий TypeScript, валидация Zod, отсутствие `any` типов
- **Разделение ответственности:** Renderer (UI) ↔ Main Process (ввод/вывод, БД, API)

### Рекомендации по реализации

- Сохранять текущие паттерны схемы (без регрессий)
- Строгая типобезопасность (Zod/TypeScript)
- Разделение ответственности (Renderer vs Main Process)
- Следовать существующим паттернам IPC и архитектуре сервисов
- Использовать Drizzle ORM для всех операций с базой данных (без необработанного SQL)
- Правильная обработка ошибок с описательными сообщениями

---

## 🔮 Долгосрочные цели (Будущие соображения)

### Поддержка Multi-Booru

- Рефакторинг `SyncService` в Provider Pattern
- Абстрагирование логики, специфичной для booru
- Поддержка Danbooru, Gelbooru и т.д.

### Двухмодульная система

- **Модуль 1: Библиотека** - Локальная база данных, избранное, галерея
- **Модуль 2: Браузер** - Встроенный Webview для нативной навигации по сайту
  - Внедрение JS (`preload`) для интеграции с сайтом
  - Оверлей с Floating Action Button (FAB) "Track Artist"

### Панель статистики

- Аналитика по отслеживаемым artists и постам
- История синхронизации и статистика
- Анализ контента

---

---

## 📝 Примечания

- Все функции должны поддерживать обратную совместимость
- Миграции базы данных должны быть тщательно протестированы
- UI/UX должен соответствовать существующим шаблонам проектирования
- Оптимизация производительности важна для больших наборов данных (6000+ постов)